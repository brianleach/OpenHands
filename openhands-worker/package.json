{
  "name": "openhands-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker deployment for OpenHands - AI-powered issue resolution",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@cloudflare/sandbox": "^0.1.0",
    "@cloudflare/workers-types": "^4.20250124.0",
    "@vitest/coverage-v8": "^2.0.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0",
    "wrangler": "^4.61.1"
  }
}
